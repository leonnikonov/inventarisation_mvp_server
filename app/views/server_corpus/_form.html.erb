<%= form_with(model: server_corpu) do |form| %>
  <% if server_corpu.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(server_corpu.errors.count, "error") %> prohibited this server_corpu from being saved:</h2>

      <ul>
        <% server_corpu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
